// STUB — replaced by Workstream C (Ink TUI for `agentpulse live`).
// See src/types.ts for the LiveOptions contract.

import type { LiveOptions } from '../types.js';

export async function runLiveTui(_opts?: LiveOptions): Promise<void> {
  throw new Error('tui.runLiveTui not yet implemented');
}
