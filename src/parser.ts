// STUB — replaced by Agent #1 (parser workstream).
// See src/types.ts for the TranscriptEvent contract.

import type { TranscriptEvent, ParseOptions } from './types.js';

export async function parseTranscript(
  _transcriptDir: string,
  _opts?: ParseOptions
): Promise<TranscriptEvent[]> {
  throw new Error('parser.parseTranscript not yet implemented');
}
