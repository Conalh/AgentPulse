// STUB — replaced by Agent #4 (narrative + CLI workstream).
// See src/types.ts for the PulseRecap contract.

import type {
  EnrichedWindow,
  OutcomeSignal,
  TrajectoryVerdict,
  PulseRecap,
} from './types.js';

export function renderRecap(
  _enriched: EnrichedWindow,
  _outcome: OutcomeSignal,
  _verdict: TrajectoryVerdict
): PulseRecap {
  throw new Error('narrative.renderRecap not yet implemented');
}
