// STUB — replaced by Workstream B (multi-session pulse orchestrator).
// See src/types.ts for the contracts.

import type {
  OrchestratorOptions,
  PulseOrchestrator,
} from './types.js';

export function createOrchestrator(_opts?: OrchestratorOptions): PulseOrchestrator {
  throw new Error('orchestrator.createOrchestrator not yet implemented');
}
